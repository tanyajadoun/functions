#include <stdio.h>

int even(int n){
    return n%2==0;
    
    
}
int main(){
    int number;
    printf("enter the number:");
    scanf("%d",&number);
     if (even(number))
    printf("%d is a even number",number);
    else
    printf("%d is a odd number",number);
    return 0;
   
}
